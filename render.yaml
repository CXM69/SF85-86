services:
  - type: web
    name: SF86-validator
    runtime: python
    plan: free
    buildCommand: "pip install ."
    startCommand: "python -m sf_validator.web"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
