services:
  - type: web
    name: sf86-validator
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m sf_validator.web"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
